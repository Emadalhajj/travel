export const INVENTORY_TYPES = Object.freeze({
  ROOM_TYPE: "roomType",

  TRIP_DEPARTURE: "tripDeparture",
  TRANSPORT: "transport",
  VISA: "visa",
  EXTRA_SERVICE: "extraService",
  VEHICLE_RENTAL: "vehicleRental",
});

export const INVENTORY_TYPE_VALUES = Object.freeze(
  Object.values(INVENTORY_TYPES),
);
