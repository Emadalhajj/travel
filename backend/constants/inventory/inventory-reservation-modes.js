export const INVENTORY_RESERVATION_MODES = Object.freeze({
  SINGLE: "SINGLE",
  PERIOD: "PERIOD",
});

export const INVENTORY_RESERVATION_MODE_VALUES = Object.freeze(
  Object.values(INVENTORY_RESERVATION_MODES),
);
