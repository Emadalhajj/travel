// Compatibility export for older imports. The implementation has one source
// of truth so language negotiation and the response contract cannot diverge.
export { errorHandler } from "../middleware/errorHandler.js";
