# Safe same-origin default. Override at build time when the API uses a separate domain.
REACT_APP_API_URL=/api

# Do not publish application source through production source maps.
GENERATE_SOURCEMAP=false
